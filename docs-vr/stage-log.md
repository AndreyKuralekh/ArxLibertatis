# Arx VR — журнал этапов

## Этап 0. Подготовка (2026-09-24)

### Сделано
- Окружение: Git, CMake 4.2, VS 2022 Enterprise 17.14 (MSVC 14.44), Ninja — уже установлены, ничего не ставили.
- Данные игры: Steam-версия (патч 1.21) скопирована в `..\gamedata\`: все `*.pak`, `Graph\`, `misc\`, `localisation\`
  (56 файлов, 737 МБ, размеры сверены). Steam-папка не изменялась.
- Форк `AndreyKuralekh/ArxLibertatis`, `upstream` = `arx/ArxLibertatis`, ветка `vr` от `upstream/master` (`5b95e4c`).
- `ArxWindows` склонирован (+ сабмодуль `data` с готовыми иконками).
- Сборка RelWithDebInfo x64 (генератор VS 2022) проходит с чистой конфигурации. Команды — в `CLAUDE.md`.
  Обходные пути без правок движка: шим `BoostConfig.cmake`; генератор VS вместо Ninja (см. `decisions.md`).
- Запуск: OpenGL 4.6 (RTX 3090), загружаются все PAK, язык текста и озвучки — русский
  (`Using text language from config: russian`, `Using audio language from config: russian`).
- Карта модулей для VR: `module-map.md`.

### Осталось / ждёт проверки
- [x] Русская озвучка в прологе — подтверждено пользователем.
- [x] Главное меню на русском — подтверждено пользователем.
- [x] Push ветки `vr` в `origin`.
- [ ] RenderDoc-снимок кадра (из плана этапа 0): перенесено в начало этапа 1, порядок проходов уже описан по коду.

### Известные проблемы и заметки
- Плановый CI upstream на `master` красный (2026-09-18); локальная сборка MSVC при этом проходит.
- Лог `arx.log` пишется буферизованно: при принудительном завершении процесса он пустой. Закрывать игру штатно.
- Сессия RDP: окно 2560×1440 @ 32 Гц, звук через «Remote Audio». Для реальной проверки звука и производительности
  нужен локальный запуск.
- Автоматически пролистать вступление до главного меню с помощью SendKeys не удалось. Меню проверил пользователь.

### Итог
Этап 0 завершён: игра собирается из форка и запускается на русском (текст и озвучка).

## Этап 1. OpenXR, стерео, UI-панель, контроллеры (начат 2026-09-24)

План: `C:\Users\Administrator\.claude\plans\1-agile-hippo.md`, ключевая идея — моно-проекция на CPU +
перепроекция `P_eye·V_eye·S⁻¹` на GPU (см. `decisions.md`, будет дописано).

### Сделано (проверено в Quest 3 через Virtual Desktop)
- 1.1 `f8e2ecd` — опция `ARX_VR`, OpenXR loader 1.1.63 (`..\OpenXR-SDK\install`), секция `[vr]`, `--vr`,
  `--vr-debug`, `--vr-dump-frames` (пока не реализован). Плоская сборка без изменений.
- 1.2 `b467c64` — сессия, swapchain'ы глаз 2688×2880 (sRGB) + UI, цикл кадра. VDXR поддерживает OpenGL.
- 1.3 `2504440` — рендер в FBO (`OpenGLRenderer::setRenderTarget`), вся игра на UI-панели 1920×1080,
  зеркало в окне. Меню на русском в шлеме — подтверждено.
- 1.4 `4390da3` — стерео мира с отслеживанием головы: мир виден, повороты головы корректны.

### Следующий шаг (продолжить отсюда)
- [ ] **Дрожание анимированных моделей и предметов** при движении головы (стены и мебель стабильны).
  Гипотеза: часть сущностей проецируется на CPU с камерой *другого момента*, чем `S` для перепроекции:
  игрок — в `EERIEDrawAnimQuatUpdate` в начале `updateLevel()` (до `updateActiveCamera()`), NPC/предметы —
  возможно, в `UpdateInter`/`bbox`-пути или в `FlyingOverObject` до `updateLevel`. Проверить: запоминать `S`,
  с которой спроецированы `vertexClipPositions`, либо перепроецировать анимированные модели в `RenderInter`
  текущей камерой. Проверить также порядок: `applyHeadPose` → `PrepareCamera` → `ARX_SCENE_Update`.
- [ ] Высота recenter в логе −0.05 м (LOCAL-пространство) — ок, но проверить масштаб/рост в шлеме.
- [ ] 1.5 голова и тело (yaw по взгляду, snap-поворот, отключить bobbing/quake/zoom, recenter по кнопке).
- [ ] 1.6 контроллеры (лазер = мышь, стик, кнопки). 1.7 дамп кадров, документы.
- [ ] Известное: 2D-частицы (PARTICLE_2D) и хак `Zpos<=1` в `EERIECreateSprite` не учитывают стерео.
